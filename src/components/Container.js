import styled from 'styled-components';
import { space } from 'styled-system';

export default styled.div`
  ${space};
  display: flex;
  align-items: center;
  flex: 1;
  max-width: 58rem;
  margin-left: auto;
  margin-right: auto;
`;
